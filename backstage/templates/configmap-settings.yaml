apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "backstage.fullname" . }}-settings
  labels:
    {{- include "backstage.labels" . | nindent 4 }}  
data:
  BACKSTAGE_BASE_URL: https://backstage.globaldnadevweudevaks-pool-aks.dna.internal.mars
  NODE_ENV: production
  GITHUB_APP_ID: "257245"
  POSTGRES_DATABASE: backstagetoolweudevdb
  POSTGRES_HOST: backstagetoolweudevpsql.postgres.database.azure.com
  POSTGRES_PORT: "5432"
  POSTGRES_USER: backstagetoolweudevpsqladmin@backstagetoolweudevpsql
  AUTH_GITHUB_CLIENT_ID: Iv1.bbd496e9e4f9c2e5
  AUTH_GITHUB_WEBHOOK_URL: http://banana # This shouldn't matter, no webhooks are actually used, but the github integration requires a value
  AUTH_GITHUB_WEBHOOK_SECRET: banana # This shouldn't matter, no webhooks are actually used, but the github integration requires a value